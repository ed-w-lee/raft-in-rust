/**
 * Manages the translation from strings to messages that can be used
 * by the nodes for state management.
 */
use rafted::Message;

pub fn parse_msgs(s: &Vec<u8>) -> Message {
	unimplemented!();
}

pub fn to_stream(msg: Message) -> Vec<u8> {
	unimplemented!();
}
